#include <stdio.h>

int main ()
{
    int a = 3; 
    int b = 4; //, e; idk what im supposed to do with e
    double f = 4, c, d;
    d = a + b;
    c = a / (f - b);
    printf ("The value of d is %d\n", d);
    return (0);
}